#pragma once

#include <bits/stdc++.h>

#define all(x) (x).begin(), (x).end()
#define make_unique(x) sort((x).begin(), (x).end()); (x).erase(unique((x).begin(), (x).end()), (x).end())
#define imie(x) #x << ": " << x
// #define repeat(n) for (int __macro_repeat_##__COUNTER__ = 0; __macro_repeat_##__COUNTER__ < n; ++__macro_repeat_##__COUNTER__)

using namespace std;
